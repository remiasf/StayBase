version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: staybase_api
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env